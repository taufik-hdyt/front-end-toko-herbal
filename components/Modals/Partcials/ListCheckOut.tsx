import { Box, Heading, Text } from "@chakra-ui/react";
import { memo } from "react";

const ListCheckOut: React.FC = (): JSX.Element => {
  return (
    <Box>
      <Heading>Totoal</Heading>
    </Box>
  );
};

export default memo(ListCheckOut);
